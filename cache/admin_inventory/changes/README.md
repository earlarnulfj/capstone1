# Admin Inventory Changes

Contains per-inventory JSON files with the latest committed state and version.
Example filename: `inventory_123.json`.

These files are produced after successful create/update actions in `admin/inventory.php` and are admin/staff only.